import styled from "styled-components";
import { Link } from "react-router-dom";

const Logo = styled(Link)`
  margin-right: 20px;
  color: #4b566b;
  font-size: 28px;
  font-family: "Comic Neue", cursive;
  font-weight: 300;
  text-decoration: none;
`;

export { Logo as LogoStyled };
